# Metric and data contracts

For every metric define operational meaning, grain, keys, numerator/denominator, population, source of record, transformations, refresh latency, quality checks, owner, audience, and response. Version changes to definition; never silently rewrite history.

Exit when users can reproduce the number and know when it is unsafe to use.
