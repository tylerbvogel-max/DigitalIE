# Local observations

This directory accepts only sanitized observations that pass the [information boundary](../framework/information-boundary.md). An observation records what was noticed; it does not establish a defect, cause, requirement, or approved practice.

Use [the observation template](../templates/local-observation.md). Move a reusable proposal into `candidates/` for explicit review. Never edit baseline guidance directly from an observation.
